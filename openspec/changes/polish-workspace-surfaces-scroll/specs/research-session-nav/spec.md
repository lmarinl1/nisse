## ADDED Requirements

### Requirement: Proceso menu uses global scrollbar standard
The research session Sidebar menu scrollbar SHALL follow the application-wide discovery capsule/circle-on-line scrollbar standard and MUST NOT introduce a divergent heavy-rail treatment.

#### Scenario: Proceso overflow matches global scroll chrome
- **WHEN** the Proceso session list overflows vertically
- **THEN** its scrollbar matches the global thin-line + discovery capsule/circle thumb standard
