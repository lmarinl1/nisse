## 1. Structure

- [x] 1.1 Wrap identity (lockup + motto), session nav, and study foot each in a `workspace__rail-panel` container in `StudyWorkspace`
- [x] 1.2 Add the motto «El futuro no se predice: se anticipa y se diseña.» under the brand lockup in the identity panel

## 2. Visual system

- [x] 2.1 Style panels with token border, radius, padding, and gap in `workspace.css` (panel language, not card shadows)
- [x] 2.2 Keep study panel foot-anchored (`margin-top: auto`) and verify mobile stacking still works

## 3. Verify

- [x] 3.1 Spot-check Workspace rail: three rounded panels, motto under NISSE, sessions and Campo foot intact (WSL `npm run dev` in `frontend/`)
