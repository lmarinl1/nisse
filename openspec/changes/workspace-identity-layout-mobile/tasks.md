## 1. Identity layout

- [x] 1.1 Restructure the Workspace identity panel into mark (left, starred official-clean, larger) + NISSE (upper right) + future motto (lower right)
- [x] 1.2 Style the identity grid with tokens; keep discovery tint on the mark

## 2. Mobile responsive

- [x] 2.1 Refine mobile Workspace shell so rail panels, Canvas, and Companion stack cleanly with framed surfaces and usable session nav
- [x] 2.2 Ensure identity composition remains readable on narrow widths

## 3. Verify

- [x] 3.1 Spot-check desktop identity layout and a mobile-width viewport (WSL `npm run dev` in `frontend/`)
